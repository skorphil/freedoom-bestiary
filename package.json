{
	"name": "freedoom-bestiary",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"test": "bun test",
		"lint": "biome check .",
		"format": "biome format --write .",
		"dev:web": "bun --filter @freedoom-bestiary/web dev --host",
		"build:web": "bun --filter @freedoom-bestiary/web build"
	},
	"devDependencies": {
		"@types/node": "^26.1.0",
		"bun-types": "^1.3.14",
		"typescript": "^6.0.3"
	}
}
